package com.example.demo.service;

import com.example.demo.entity. AlertSchedule; ;

public interface AlertScheduleService {

    AlertSchedule postdata4(AlertSchedule alertSchedule);
    
   AlertSchedule getdata4(Long id); 

}
